## 一、准备条件

- Windows 10/11
- 能访问国内网络（DeepSeek / GLM 均为国内直连，无需翻墙）
- 已拿到 DeepSeek 和 GLM 的 API Key

------

## 二、第 1 步：安装 Node.js（必须 ≥ 22）

Claude Code 要求 Node.js 22 或更高版本。

1. 打开 https://nodejs.org/ 下载 LTS 版（或当前稳定版）
2. 安装时一路下一步（默认勾选添加到 PATH）
3. 验证（新开一个终端）：

```powershell
node -v
npm -v
```

看到 `v22.x.x` 或更高即可（`npm -v` 一般在 10+）。

> 如果已经装了但版本低于 22，请先升级 Node.js。

------

## 三、第 2 步：配置 npm 国内镜像（加速下载）

```powershell
npm config set registry https://registry.npmmirror.com
```

验证：

```powershell
npm config get registry
```

应显示 `https://registry.npmmirror.com`。

> 说明：PowerShell 5 不支持 `&&` 连接命令，请分开逐条执行。

------

## 四、第 3 步：安装 Claude Code CLI

```powershell
npm install -g --allow-scripts=@anthropic-ai/claude-code @anthropic-ai/claude-code
```

> 新版本 npm 会默认阻止 postinstall 脚本，必须加 `--allow-scripts=@anthropic-ai/claude-code` 让它装成原生二进制。

验证：

```powershell
claude --version
```

应显示类似 `2.1.233 (Claude Code)`。

------

## 五、第 4 步：复制配置目录

把当前这台电脑的整个 `D:\ClaudeCode` 文件夹，完整复制到新电脑的**同一路径**：

```javascript
D:\ClaudeCode
```

目录内容应包含：

```javascript
cc.cmd               # 启动器（核心）
cc-config.json       # 供应商配置（核心，含 API Key）
cc-config-read.ps1   # 配置读取脚本
cc-provider.ps1      # 供应商管理脚本
cc-history.ps1       # 历史会话查看
cc-clear.ps1         # 会话清理
cc-sessions.mjs      # 会话管理网页服务器
cc-sessions-ui.html  # 会话管理网页界面
cc-backup.ps1        # 会话备份脚本
cc-role.ps1          # 角色系统启动器
roles/               # 角色数据（人设、知识库、会话记录）
settings.json        # Claude Code 用户设置
README.md            # 使用说明
projects/            # 历史会话（可选，想续接就一起复制）
sessions/            # 会话元数据（可选）
cache/               # 缓存（可不复制）
```

> ⚠️ **路径要求**：所有脚本内部都写死了 `D:\ClaudeCode` 这个绝对路径。 因此新电脑上**必须也放在 `D:\ClaudeCode`**，否则要逐个修改脚本里的路径。 如果新电脑没有 D 盘，请先改脚本路径再复制，或新建一个 D 盘分区。

> 📦 **会话备份**：`D:\ClaudeCode-archive` 里放着 `projects/`、`file-history/`、`tasks/`、`history.jsonl` 的镜像（每日 03:00 计划任务 `CC-Config-Backup` 自动更新，也可 `cc backup` 手动备份）。迁移时把这个目录一起复制，即可保留全部历史会话。`settings.json` 里已设 `cleanupPeriodDays: 99999` 防止 Claude Code 自动清理。

------

## 六、第 5 步：配置环境变量

打开 PowerShell（普通用户即可），执行：

```powershell
setx CLAUDE_CONFIG_DIR "D:\ClaudeCode"
```

这会让 Claude Code 把会话、缓存、设置都写到 `D:\ClaudeCode`。

> 注意：`setx` 只对**之后新开**的终端生效。当前终端不会立即生效。

------

## 七、第 6 步：把启动器加入 PATH

两种方式任选其一：

### 方式 A（推荐，图形界面）

1. 按 `Win` 键，搜索「环境变量」
2. 点「编辑系统环境变量」→「环境变量」
3. 在「用户变量」里选中 `Path` →「编辑」
4. 新建一行，填入 `D:\ClaudeCode`
5. 确定保存

### 方式 B（PowerShell 命令）

```powershell
$old = [Environment]::GetEnvironmentVariable("Path", "User")
[Environment]::SetEnvironmentVariable("Path", $old + ";D:\ClaudeCode", "User")
```

------

## 八、第 7 步：验证

**关闭所有旧终端，重新开一个新终端**，然后依次执行：

```powershell
# 1. 查看帮助（能显示说明命令就对了）
cc help

# 2. 查看供应商和当前模型
cc provider

# 3. 一次性提问（真正调用 DeepSeek，验证全链路）
cc -p "请只回复：安装成功"
```

预期：

- `cc help` 打印命令列表
- `cc provider` 显示 `deepseek [current]` 和 `glm`
- `cc -p ...` 返回「安装成功」（会伴随 `unrecognized_model` 黄色提示，属正常现象）

------

## 九、日常使用速查

| 命令                          | 作用                                   |
| :---------------------------- | :------------------------------------- |
| `cc`                          | 用当前供应商启动（正常权限）           |
| `cc danger`                   | 危险模式（跳过权限确认，可全自动托管） |
| `cc provider`                 | 列出供应商 + 当前模型                  |
| `cc provider switch glm`      | 一键切到 GLM                           |
| `cc provider switch deepseek` | 一键切回 DeepSeek                      |
| `cc mode`                     | 同 `cc provider`                       |
| `cc hist`                     | 查看历史会话                           |
| `cc clear` / `cc clear all`   | 清理会话                               |
| `cc resume [id]`              | 恢复历史会话                           |
| `cc ui`                       | 网页版会话管理（浏览/批量删除/角色）   |
| `cc backup`                   | 手动备份会话到 `D:\ClaudeCode-archive` |
| `cc role <name>`              | 以某角色启动会话（人设+知识库）        |
| `cc role <name> --from <ids>` | 启动角色会话并继承指定会话             |
| `cc roles`                    | 列出所有角色                           |
| `cc -p "问题"`                | 一次性提问                             |

会话内切换模型（上下文连续，最省 token）：

```javascript
/model deepseek-v4-pro     # 切强模型思考
/model deepseek-v4-flash   # 切便宜模型执行
```

网页版会话管理（批量查看 / 删除历史会话）：

```powershell
cc ui   # 打开 http://127.0.0.1:18080，仅本机可访问，Ctrl+C 停止
```

------

## 十、常见问题

**Q1：`cc` 提示找不到命令？** → 没加 PATH，或没关旧终端。按第 6、7 步重做，并重启终端。

**Q2：`claude` 提示命令不存在？** → 第 3 步没装成功，或 Node 版本低于 22。重新安装。

**Q3：`cc -p` 报模型不存在？** → 检查 `D:\ClaudeCode\cc-config.json` 里 model 和 baseUrl 是否属于同一家供应商（例如 deepseek 的 model 不能用 glm 的 baseUrl）。

**Q4：`unrecognized_model` 黄色提示？** → 正常现象，第三方模型必现，不影响使用。

**Q5：想用别的模型/供应商？** → 编辑 `D:\ClaudeCode\cc-config.json`，在 `provider config` 里加一段即可，详见 `README.md`。国内直连的可选 baseUrl 速查表也在 README 里。 </｜｜DSML｜｜parameter> </｜｜DSML｜｜invoke> </｜｜DSML｜｜tool_calls>